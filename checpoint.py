import random
import time

def calculate_sum(n):
    """
    计算从 1 到 n 的累加和，用于演示循环中的断点
    """
    print(f"--- 开始计算 1 到 {n} 的累加 ---")
    total = 0
    for i in range(1, n + 1):
        # 💡 断点建议：可以在下面这一行打断点，观察 total 和 i 的变化
        total += i
        if i % 5 == 0:
            print(f"进度: 已处理到 {i}")
    return total

def process_data(data_list):
    """
    处理列表数据，用于演示条件断点和异常观察
    """
    results = []
    for item in data_list:
        # 💡 断点建议：在这里打断点，查看当前 item 的值
        if item > 80:
            status = "High"
        elif item > 40:
            status = "Medium"
        else:
            status = "Low"
        
        results.append({"value": item, "status": status})
    return results

def main():
    """
    主函数：程序的入口
    """
    print("Python 断点调试学习示例已启动")
    
    # 示例 1：简单变量观察
    username = "Trae User"
    lucky_number = random.randint(1, 100)
    print(f"你好 {username}, 你的幸运数字是 {lucky_number}")

    # 示例 2：进入函数内部
    # 💡 调试技巧：运行到这一行时，使用 "Step Into (F11)" 进入函数内部
    sum_result = calculate_sum(10)
    print(f"累加结果: {sum_result}")

    # 示例 3：处理复杂数据结构
    raw_data = [random.randint(1, 100) for _ in range(5)]
    # 💡 调试技巧：在下一行打断点，运行到此处后查看变量面板中的 raw_data
    processed_results = process_data(raw_data)
    
    for res in processed_results:
        print(f"数值: {res['value']}, 状态: {res['status']}")

    print("程序执行完毕！")

if __name__ == "__main__":
    main()

# === 断点调试 (Debug) 学习指南 ===
# 1. 打断点：在代码行号的左侧点击，会出现一个红色圆点。
# 2. 启动调试：点击 IDE 右上角的“调试”图标（虫子形状），或按 F5。
# 3. 调试控制栏说明：
#    - 继续 (F5): 运行到下一个断点。
#    - 逐过程 (F10): 运行下一行代码，但不进入函数内部。
#    - 逐语句 (F11): 进入函数内部。
#    - 跳出 (Shift+F11): 跳出当前函数，回到调用处。
#    - 重启 (Ctrl+Shift+F5): 重新开始调试。
#    - 停止 (Shift+F5): 结束调试。
# 4. 变量观察：程序暂停时，在左侧的 "变量 (VARIABLES)" 面板可以实时查看和修改变量的值。
# 5. 监视 (WATCH)：你可以手动添加想要持续关注的变量或表达式（例如：total > 10）。
